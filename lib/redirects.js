// Centralized redirect registry for future slug changes
// Add entries here when course or page slugs change to preserve SEO equity
module.exports = {
  redirects: [
    // Example: { source: '/old-slug', destination: '/new-slug', permanent: true },
  ]
}
